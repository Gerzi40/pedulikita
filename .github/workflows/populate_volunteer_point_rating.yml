name: Populate Volunteer Point Rating

on:
  schedule:
    - cron: "0 0 * * *" 
  workflow_dispatch:

jobs:
  ping_daily:
    runs-on: ubuntu-latest
    steps:
      - name: Populate Volunteer Point Rating
        run: curl -X GET https://peduli-kita.vercel.app/cron/populate-volunteer-point-rating
